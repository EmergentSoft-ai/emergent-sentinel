# ═══════════════════════════════════════════════════════════════
# SENTINEL MIND — requirements.txt
# Pin exacto de versiones para reproducibilidad en producción
# Instalar: pip install -r requirements.txt --break-system-packages
# ═══════════════════════════════════════════════════════════════

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0       # standard incluye uvloop + httptools
python-multipart==0.0.9

# ── HTTP Client asíncrono ─────────────────────────────────────
httpx==0.27.0

# ── Environment & Config ──────────────────────────────────────
python-dotenv==1.0.1

# ── Seguridad ─────────────────────────────────────────────────
pycryptodome==3.20.0
cryptography==42.0.7

# ── Validación y Settings ─────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Logging estructurado (JSON en producción) ─────────────────
structlog==24.1.0

# ── Retry automático para llamadas a APIs externas ────────────
tenacity==8.3.0

# ── Performance: event loop mejorado (reemplaza asyncio default)
uvloop==0.19.0
